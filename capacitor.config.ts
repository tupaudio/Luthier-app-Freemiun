import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tupaaudio.luthierdebolso',
  appName: 'Luthier de Bolso',
  webDir: 'dist'
};

export default config;
